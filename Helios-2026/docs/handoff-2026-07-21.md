# Handoff — full-code review, 2026-07-21

For the next agent (or human) resuming work on Helios-2026. Written after a full review of
every subsystem, the keybinds, and all outstanding TODOs. State at hand-off:

- Branch `swerve-fixes` at **`a808ba8`** — the 2026-07-18 on-robot fixes (hood calibration,
  hopper reset-config, TunerConstants power budget) and the LimelightThermalManager are now
  **committed**; nothing load-bearing sits uncommitted.
- `gradlew build` green, all unit tests pass (PathPlanner file parse, LimelightThermal,
  MatchStatus).
- Keybinds verified against the team spec 2026-07-16 (+07-17/07-18 amendments) — the
  authoritative binding list is the comment block in `RobotContainer.configureBindings()`.
  **Do not change bindings without a team directive**, and mirror any change into the
  driver-companion CONTROLS panel (see note at the top of `configureBindings`).

> **STATUS UPDATE (later on 2026-07-21): issues 2–5 below are FIXED** — hopper ramp
> `e3e9bdc`, Sensors stub deletion `1a88475`, dead-code cleanup `9c9400c`, 38° shot-model
> refit + `ShotModelTest` pins `4284857` (raised MIN_SCORE_DISTANCE 3.0 → 3.5 m — tell the
> drivers the close dead zone grew: back off ~3 m from the hub face). Build + all 15 tests
> green after each.
>
> **SECOND UPDATE (same day, 2026-07-21): issue 1 is ALSO resolved — but not by writing
> code.** Team confirmed in conversation: the CAN 22 SparkMax (in code as
> `intakeSliderMotor`) **IS** the jackshaft, not a separate 3rd motor — the "blocked on a
> team-assigned CAN ID" premise was wrong from the start; nothing was ever missing. See
> the corrected §1 below and `Hardware-Data-Sheet.md` §2/§4.4/§10/§11. **All 5 numbered
> issues in this doc are now resolved.** Remaining open work lives in the device-side
> checklist and on-robot verify list further down, plus one new feature added the same
> day (test-mode controller bindings — see
> `docs/superpowers/specs/2026-07-21-test-controller-bindings-design.md`). The
> C:\FRC\CLAUDE.md "No-New-Issues Rule" (added same day) governs all further changes.

## Ground rules (non-negotiable, from `C:\FRC\CLAUDE.md`)

1. Invoke the **`frc-robot-coder` skill** before touching robot code, every session.
2. **Interview the user** for any unknown measurement/current/CAN ID — never invent values.
3. Old tree at `C:\FRC\extracted\2026-Helios-Code-main\` is reference-only, never copy.
4. Hardware truth lives in `C:\FRC\Helios-2026-Robot-Data\Hardware-Data-Sheet.md`
   (§2 CAN map, §7 current limits, §11 open items).
5. Backup tree `C:\FRC\Helios-2026-backup-2026-07-18\` = the 07-17/18 on-robot session.
   Features listed below as "port from backup" should be **ported from there, not rewritten**.

## Issues to fix (priority order)

### 1. ~~Intake jackshaft motor — BLOCKED on team answer~~ RESOLVED 2026-07-21: premise was wrong
✅ **The text below was wrong — do not act on it.** Team confirmed in conversation
2026-07-21: the SparkMax at CAN 22 (in code as `intakeSliderMotor`) **IS** the jackshaft —
there is no separate 3rd intake motor, and no CAN ID was ever missing. The intake is (and
always was) a 2-motor design: rollers (TalonFX, CAN 18) + slider/jackshaft (SparkMax, CAN
22), both already fully in code. **Nothing to build here.** Corrected in
`Hardware-Data-Sheet.md` §2/§4.4/§10/§11 and `C:\FRC\CLAUDE.md`.

<details><summary>Original (incorrect) text, kept for history</summary>

The intake is a confirmed 3-motor design; code has only rollers (TalonFX, CAN 18) and
slider (SparkMax, CAN 22). The **NEO 2.0 jackshaft (SPARK MAX) is not in code** because its
CAN ID was never assigned (needs 24+, flashed via REV Hardware Client).
**Ask the team for the CAN ID first.** Then in `IntakeSubsystem`:
SparkMax + `smartCurrentLimit` + idle mode from Hardware-Data-Sheet §7 (conservative-start
column; do NOT guess), spin it with the rollers in INTAKE/OUTTAKE states, stall detection via
`getOutputCurrent()` + `Debouncer` like the slider. Ask the team which direction it must turn
relative to the rollers (or leave inversion as an on-robot TODO).

</details>

### 2. Shot model stale at 38° — desk-fixable now
`SubsystemConstants.ShooterSubsystemConstants`, "SHOT MODEL" block (`TODO STALE AT 38 DEG`):
the SCORE drag multipliers, ToF fits, and `MIN_SCORE_DISTANCE_METERS = 3.0` were all derived
at hood 44.5°, but `MAX_ANGLE` is now 38 (belt-skip inset). The descending-entry check barely
passes at 3.0 m (~2.5% margin vs ~29%).
Fix: re-run the fit generator **`docs/shot-model/ballistics.py`** (see its README) at 38° and
update `SCORE_DRAG_MULT_*`, `*_TOF_*`, `MIN_SCORE_DISTANCE_METERS`; delete the STALE comment.
Alternative (team choice): port the varying-angle shot tables (`SCORE/FEED_*_BY_DIST`) from
the backup, which sidestep the fixed-angle assumption entirely.

### 3. Hopper belt inrush ramp — one line
`HopperSubsystem` constructor: `hopperConfig` (the belts) lacks the 0.25 s open-loop ramp
every sibling motor has. Add `hopperConfig.openLoopRampRate(0.25);` before the two
`configure(...)` calls. (Flagged in `docs/power-limiting-review.md` as the one cheap nit.)

### 4. Beam-break sensors — stub lies on the dashboard
`subsystems/utility/Sensors.java` hardcodes `true`, so "Fuel Detected Indexer" on the Hopper
tab is always true. Beam-breaks are on DIO 8/9 per the data sheet but were **never physically
confirmed wired** (§11 item 11). Either construct `new DigitalInput(8/9)` once the team
confirms wiring + polarity (interview!), or delete the stub + dashboard entry until then.
Nothing gates on it today — display-only.

### 5. Cosmetic / dead code (batch these into one cleanup commit)
- `IntakeSubsystem.currentState` is never written after the constructor — "Current Intake
  State" dashboard entry is frozen at STOW. Set it alongside the actuation in `periodic()`
  or delete the entry. Unused fields `enableComp`/`enableTest` in the same file.
- `CommandSwerveDrivetrain.driveWithAimLockCommand(...)` is unused since the 07-17 RT
  aim-then-freeze redesign (RobotContainer comment says so). Delete or keep deliberately.
- `RobotContainer.shoterSS` typo (rename to `shooterSS` if bored; compiles fine as-is).

## Deliberately NOT built (don't "fix" these)

- **Voltage-adaptive load-shed supervisor** — the one power lever left open in
  `docs/power-limiting-review.md`. GATE: first confirm late-match brownouts still reproduce
  on a **healthy, load-tested battery**; battery health is the dominant documented cause.
- 21 other power "optimizations" were adversarially rejected in that doc — most would undo
  documented on-robot fixes (steer/flywheel fold-back values, rotation slew, etc.). Read it
  before touching any current limit.
- Idle-pipeline switching in `LimelightThermalManager` is intentionally disabled
  (`LL_PIPELINE_IDLE = 0`) until pipeline 1 exists on the camera — see below.

## Port-from-backup list (only if the team asks)

In `C:\FRC\Helios-2026-backup-2026-07-18\` and nowhere else: varying-angle shot tables,
shot-time vision quality gates (isInsideField / hasRecentVisionFix / SHOT_MAX_TAG_DIST /
YAW_RATE), refuseShot envelope-edge staging, 0.88 teleop speed cap, RT drive-lock comment
block, Limelight fiducial-ID filter override, ToF refits.

## Device-side / hardware checklist (not code — needs the robot or the team)

- **Limelight web UI** (`limelight-knight.local:5801`): set the **camera mount pose**
  (prerequisite for trustworthy botpose/MegaTag2 — code assumes it); create **pipeline 1**
  (clone of 0, lower res, LEDs off) then set `LL_PIPELINE_IDLE = 1`; standing team rule:
  **every pipeline, exposure 270 / black level offset 30**.
- **Battery**: load-test; the brownout history may be a tired pack, not software.
- **Kicker breaker size** (Victor SPX has no current sensing — breaker is the CIM's only
  stall protection). Ask the team.
- Weigh the robot (PathPlanner still uses hand-entered 63.5 kg); resolve the 2nd Pigeon
  (CAD shows two, code uses CAN 23 only).

## On-robot verify list (already `TODO`-tagged in code — test in this order)

1. Shooter C follower direction (`MotorAlignmentValue.Opposed`, set 07-17 — confirm it no
   longer fights).
2. Hood encoder anchors (`HOOD_RAW_AT_FULL_UP/DOWN`) — hand-move to both stops, robot
   disabled, read "Hood Encoder Raw (rot)".
3. Hopper belts feed direction (hold B; flip BOTH inversions together if backward).
4. Slider extend sign (negative = extend assumed).
5. `TAG_LATERAL_OFFSET_SIGN` (0.0 = disabled) — verify vs botpose, then set ±1.0.
6. Heading-servo PID (5.0 P shared by all aim/align commands) + search rate.
7. Steer supply 30 A watch item: if modules hesitate mid-swing, step 30→35→40
   (`TunerConstants` comment has the history).
8. Then ballistics: `SHOT_EFFICIENCY` first, drag slopes last (tuning order is in the
   constants comments).

Developed by Team 9704 with Claude Code.
